<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/silver</item>
        <item name="colorPrimaryDark">@color/onyx</item>
        <item name="colorAccent">@color/roseGold</item>
        <item name="colorControlHighlight">@color/silver40</item>
    </style>

</resources>
